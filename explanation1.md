This solution is similar to a binary search, which takes log(n) run time. This solution has the same worst case run time, and therefore meets the O(log(n)) required runtime.

Time: O(log(n))
space: O(1)